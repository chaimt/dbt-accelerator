version: 2
bad)models:
- name: my_third_dbt_model
  description: A starter dbt model
  columns:
  - name: id
    description: The primary key for this table
    tests:
    - unique
    - not_null
  config:
    tags:
    - test
